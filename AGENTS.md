# Agent 协作约定（Cursor / Codex）

本仓库同时可能在 **Cursor** 与 **Codex** 上改。人类负责产品判断与学习节奏；Agent 负责小步实现。两边都以 **Git 远程** 为唯一真相。

## 1. 同步规则（必须）

1. 开工前：`git pull`（或 fetch + rebase/merge 到当前分支）。
2. 告一段落：`git add` 明确文件 → `commit` → `push`。
3. **换工具前先 push**；回到另一工具前先 `pull`。
4. 同一时段默认 **只有一个 Agent 写代码**；另一个只读或只改文档。
5. 不要各维护一份互不同步的目录副本；统一用本仓库（`echo-ai-product-engineering`）。

## 2. 分工建议

| 角色 | 负责 |
| --- | --- |
| 人类 | 标注标准、query 标注、dogfood 验收、上线/不上线决策、学习节奏 |
| Cursor | 扩展 UI、Probe/文档、小步产品改动、仓库结构 |
| Codex | 评测 runner、对照实验脚本、报告生成（**在人类标完或定好标准后**） |

冲突时：人类决定；不要两个 Agent 同时大改同一文件（尤其 `extension/lib/echo-related.ts`、`extension/sidepanel.tsx`）。

## 3. 工作方式（学习优先）

1. **先说明再改**：用几句话写清要动什么、不动什么、怎么验收；人类确认后再改代码。
2. **小步**：一次只做一个可理解的切口；禁止在人类未看过方案时整包落地多 Wave。
3. **保留思考痕迹**：评测/召回相关改动要能讲清「为什么这样标 / 为什么不上线」，不要只堆功能。
4. Plan / 方案若人类表示没看过：停止实现，先把方案原文给人类。

## 4. 产品红线（不要擅自做）

- 未经验证不要把向量/Hybrid 接进扩展主线。
- 不要为简历临时搭云端后端 / 鉴权 / 多租户。
- 不要改成全自动记忆 Agent；保持人在回路、低打扰、可解释。
- 不要提交密钥、`data/instance.json`、未脱敏的真实对话导出、embedding 私有缓存。
- 不要伪造用户规模或生产 SLA。

## 5. 扩展加载

Chrome 加载的是本仓库 `extension` 的 build 产物（或指向它的 junction）。  
两边都在本仓库执行 `npm run build` / `npm run dev`，不要混用旧 mono-repo `solo-founder-os` 的扩展目录当主开发路径。

## 6. 提交习惯

- 信息写清「做了什么 / 为什么」（中英文均可，短而具体）。
- 只 stage 相关文件；不把 `node_modules/`、`build/`、`.plasmo/`、本地缓存推进 Git。
- 需要人类审阅的大改动：先保持可回退（小 commit），不要一次巨型提交塞满无关文件。

## 7. 开干前自检（Agent）

- [ ] 已 pull，工作区干净或已知冲突
- [ ] 人类已确认本步范围（或明确授权「按某文档第 X 节做」）
- [ ] 知道本步验收方式
- [ ] 不会动红线列表里的项
